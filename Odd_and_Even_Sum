num = int(input())


def odd_and_even(a):
    a = str(a)
    sum_even = 0
    sum_odd = 0
    for i in a:
        i = int(i)
        if i % 2 == 0:
            sum_even += i
        else:
            sum_odd += i
    return print(f"Odd sum = {sum_odd}, Even sum = {sum_even}")


odd_and_even(num)

